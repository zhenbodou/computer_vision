# ============================================================
#  用 Rust 玩转机器视觉 —— .gitignore
# ============================================================

# --- Rust 编译产物 ---------------------------------------------
# 匹配任意层级的 target/（顶层 code/target、各 dl_labs/*/target、
# projects/*/target 等），这是仓库体积的绝对大头。
target/
**/*.rs.bk
*.pdb

# Cargo.lock 是【故意保留】的：本书示例都是可执行程序，锁定依赖
# 版本能保证读者复现出完全一致的构建结果。请勿在此忽略它。

# --- mdbook 构建输出 -------------------------------------------
# `mdbook build` 生成的静态站点，可随时重新生成。
/book/

# --- 示例运行时生成的图片 / 数据 --------------------------------
# 书里展示的是「保存这些文件的代码」，并未把图片嵌进正文，
# 因此它们是纯产物，跑一遍示例即可重建。
out_*.png
out_*.jpg
out_*.jpeg
out_*.gif
out_*.bmp
out_*.csv

# --- 训练 / 导出的模型权重 --------------------------------------
# 由各深度学习实验（train / finetune / export）生成，属可再生产物。
# 若某个权重是需要随仓库分发的，请为其单独加一行 `!path/to/model.safetensors`。
*.safetensors
*.onnx
*.pt
*.pth
*.ckpt
*.gguf
*.mpk
*.npy
*.npz

# --- WASM 打包输出 ---------------------------------------------
# wasm-pack 生成目录。
code/wasm_demo/pkg/

# --- 日志 / 本地数据库 -----------------------------------------
*.log
*.tmp
*.sqlite
*.sqlite3
*.db

# --- 操作系统文件 ----------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# --- 编辑器 / IDE ----------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~
