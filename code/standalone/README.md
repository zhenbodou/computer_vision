# 独立示例

此目录中的源码需要单独创建 Cargo 工程运行，不参与 `code/` 主工程的
`cargo check --examples`。原因不是示例不可验证，而是深度学习、GPU 和视频栈
通常需要互斥的 feature、平台运行库或体积很大的依赖。

每个文件开头必须给出经过验证的依赖版本、运行命令、硬件要求和预期输出。
CI 应为这些示例建立独立 job，不能用“独立工程”作为不测试的理由。
