[package]
name = "ch105_pose_counter"
version = "0.1.0"
edition = "2021"

# 空的 [workspace] 让本工程成为独立工作区，不被上层 code/Cargo.toml 收编。
[workspace]

[dependencies]
candle-core = "0.11.0"
candle-nn = "0.11.0"
image = "0.25"
imageproc = "0.27"
