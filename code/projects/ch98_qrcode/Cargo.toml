[package]
name = "ch98_qrcode"
version = "0.1.0"
edition = "2021"

# 空的 [workspace] 让本工程成为独立工作区，不被上层 code/Cargo.toml 收编。
[workspace]

[dependencies]
image = "0.25.10"
imageproc = "0.27"
qrcode = "0.14.1"
rqrr = "0.10.1"
